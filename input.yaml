Configuration:
    Structure:
        folder: vox_lib
        voxel_file: square_100nm_100nm_voxel.txt
        #voxel_file: layers_50nm_50nm_voxel.txt
        NV_method: True                # Normal Vector Method (it is always true for now)

    Optics:
        nklib: nk_lib
        wavelength: [500, 500, 5]      # 250 ~ 1000 nm by 5nm
        polar_angle: [0, 0, 5]       # 0 ~ 10 degree by 5
        azimuthal_angle: [0, 0, 5]   # 0 ~ 10 degree by 5
        polarization: 1              # pol x TE(s) : (1-0.5) x TM(p)
        harmony_x: 1 
        harmony_y: 1 
        #dop: 1
